# ===== Frontend env (Vite) =====
# Local dev API origin (no /api needed; api.ts appends it).
VITE_API_URL=http://localhost:5050
# For production (Vercel): set this in Vercel Project → Environment Variables
# VITE_API_URL=https://luxlather-backend.onrender.com

# ---- Currency (UI defaults) ----
# Must be one of the backend SUPPORTED_CURRENCIES.
VITE_DEFAULT_CURRENCY=USD
# Comma-separated list (order controls dropdown order on UI if you use it).
VITE_SUPPORTED_CURRENCIES=JPY,USD,EUR,KES,RWF,TZS

